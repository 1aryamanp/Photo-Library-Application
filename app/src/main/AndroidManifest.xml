<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.photosapp21">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/detail_screen_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Photos21">
        <activity android:name="com.example.photosapp21.PhotoDetailView"></activity>
        <activity
            android:name="com.example.photosapp21.PhotoView"
            android:label="@string/title_activity_photo_view"
            android:parentActivityName="com.example.photosapp21.MainActivity"
            android:theme="@style/Theme.Photos21.NoActionBar">

            <!-- <meta-data -->
            <!-- android:name="android.support.PARENT_ACTIVITY" -->
            <!-- android:resource="com.example.MainActivity" /> -->
        </activity>
        <activity android:name="com.example.photosapp21.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>